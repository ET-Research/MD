namespace eval ::namd::rx {namespace export exchange}

#-------------------------------------------------------
# Perform replica exchange
#-------------------------------------------------------
proc ::namd::rx::exhange {namd_keys namd_values} {
    set now [dict create {*}[]]
    set potential $namd_kesy()
}